# AI Assistant Rules for CopyrightVisualMonitor 统一项目
# 本项目合并了两大功能模块：① 软著申请状态监控 ② 软著文件自动上传提交
# 技术栈：Python 3.12+ / ttkbootstrap GUI / Playwright + Selenium / OpenCV OCR / openpyxl Excel

---

## 1. Systematic Debugging（系统化调试）
**核心铁律: 先定位根因，再修复代码。**
*   **禁止盲修:** 不允许在未分析根因的情况下提交任何修复，包括"试试看"式的快速补丁。
*   **完整调查流程:** 仔细阅读错误信息 → 稳定复现 → 检查最近变更 → 追踪数据流 → 形成单一假设 → 最小化测试 → 验证。
*   **3次失败规则:** 如果同一问题连续尝试 3 次以上修复仍不奏效，立即停止并重新审视架构设计，与用户讨论。
*   **单一修复原则:** 每次只修复一个根因，不要在修 bug 时顺带重构其他代码。
*   **多组件系统诊断:** 当系统涉及多个组件（GUI → 浏览器 → 页面 → OCR → 数据导出），在每个组件边界添加诊断日志，先定位故障组件再深入分析。

## 2. Playwright Web Automation（浏览器自动化）
**核心铁律: 构建健壮、容错的自动化脚本。**
*   **等待策略:** 优先使用 `waitForURL`、`waitForSelector`、`waitForLoadState` 等条件等待，严禁使用硬编码 `sleep()` 或任意超时。
*   **错误处理:** 所有关键自动化步骤（元素定位、点击、表单填充）必须包裹在 `try-except` 中，提供有意义的错误日志。
*   **智能降级:** 若精确 XPath 失败，降级到基于语义文本的元素定位（如 `locator(...).filter(has_text="...")`）。
*   **浏览器上下文管理:** 操作多标签页或弹出窗口时，显式管理浏览器上下文，及时关闭不需要的页面，防止 "Target closed" 崩溃。
*   **可见模式默认:** 除非明确要求 headless，始终使用 `headless=False` 以便人工观察和调试。
*   **滑块验证码处理:** 使用 OpenCV 模板匹配精确定位缺口位置，模拟人类拖拽轨迹（加速→匀速→减速），失败时最多重试 3 次。

## 3. Computer Vision & OCR（计算机视觉与 OCR）
**核心铁律: 精准的视觉识别是数据可靠性的基础。**
*   **截图与裁剪:** 使用稳定的屏幕坐标系统截取目标区域，裁剪前验证坐标范围有效性。
*   **OCR 预处理:** 对截图进行灰度化、二值化、降噪等预处理以提升 Tesseract / PaddleOCR 识别准确率。
*   **调试输出:** 关键的 OCR/图像处理步骤必须保存中间结果图片（如 `debug_crop.png`、`debug_edges.png`）供人工校验。
*   **坐标映射:** 在从截图坐标映射到屏幕坐标或 DOM 坐标时，仔细处理 DPI 缩放和偏移量。
*   **文本清洗:** OCR 结果需经过正则清洗、空格/换行标准化后再使用。

## 4. Python 3.12+ Modern Practices（现代 Python 实践）
**核心铁律: 编写干净、带类型注解、符合 Python 惯用法的代码。**
*   **类型注解:** 所有函数参数和返回值都要有 type hints。
*   **代码风格:** 遵循 PEP 8，函数保持单一职责，优先可读性和可维护性。
*   **精确异常处理:** 禁止裸 `except:`，捕获具体异常类型并输出有意义的错误日志。
*   **标准库优先:** 优先使用 Python 标准库（`re`、`time`、`logging`、`subprocess`、`json`、`os`、`pathlib`），仅在核心功能需要时引入外部依赖。
*   **线程安全:** GUI 操作必须通过 `root.after()` 调度到主线程；长时间操作使用 `threading.Thread(daemon=True)` 在子线程运行。
*   **配置管理:** 使用 `config.json` 统一管理账号密码和运行参数，配置读写通过 `config_manager.py` 模块完成。

## 5. ttkbootstrap GUI Design（现代桌面 GUI 设计）
**核心铁律: 界面美观、交互直觉、状态清晰。**
*   **主题风格:** 使用 ttkbootstrap `superhero` 暗色主题，保持现代深色科技感。
*   **模块切换:** UI 需支持两种功能模式切换：① 软著申请状态监控 ② 软著文件自动上传提交。切换时刷新对应的控制面板和启动逻辑。
*   **身份凭据:** 无论哪种模式，都共享同一套账号密码配置卡片，支持显示/隐藏密码、一键保存。
*   **实时日志:** 右侧终端面板实时显示运行日志，使用等宽字体和绿色字符营造终端氛围。
*   **状态反馈:** 使用进度条（indeterminate）、状态文本和按钮禁用态清晰反馈运行状态。
*   **字体系统:** 标题使用 "Microsoft YaHei UI" bold，正文 10pt，终端日志使用 "Consolas"。

## 6. Excel Data Export（Excel 数据导出）
**核心铁律: 使用 Excel 公式而非 Python 计算硬编码值。**
*   **动态公式:** 所有汇总、统计字段都应使用 Excel 公式（`=SUM()`、`=AVERAGE()` 等），保持电子表格的动态可更新性。
*   **openpyxl 使用:** 操作已有 xlsx 文件时使用 `load_workbook()` 保留原有格式，新建时使用 `Workbook()`。
*   **格式规范:** 日期列使用标准格式，数字列设置适当宽度，标题行加粗并设置背景色。
*   **错误检查:** 保存后检查是否存在 `#REF!`、`#DIV/0!` 等公式错误。

## 7. Workflow Automation（工作流自动化）
**核心铁律: 流程分阶段执行，每个阶段有明确的成功/失败判定。**
*   **顺序编排:** 主流程分为明确阶段（初始化浏览器 → 登录验证 → 页面导航 → 数据采集/表单填写 → 数据导出/提交 → 结果归档）。
*   **状态回调:** 每个阶段的状态变化都通过 GUI 回调（`log()`、`update_status()`）实时反馈。
*   **阻塞式验证:** 需要人工介入时（如滑块验证码），使用 `threading.Event` 阻塞子线程，等待用户 GUI 操作确认后继续。
*   **幂等性:** 关键操作应考虑幂等设计，避免重复提交或重复创建记录。

## 8. Error Handling Patterns（错误处理模式）
**核心铁律: 优雅降级，绝不静默失败。**
*   **分层捕获:** 每个功能模块（浏览器操作、OCR 识别、文件 I/O、网络请求）独立包裹异常处理。
*   **用户友好提示:** 异常信息通过 GUI 日志和弹窗两种方式呈现，既有技术日志也有用户可理解的提示。
*   **恢复策略:** 非致命错误（如单次 OCR 失败）应重试；致命错误（如浏览器崩溃）应安全退出并释放资源。
*   **Finally 块清理:** 无论成功还是失败，finally 块中恢复按钮状态、停止进度条。

## 9. Windows PowerShell & Batch Scripts（Windows 脚本）
**核心铁律: 遵循严格的 Windows 执行模式。**
*   **路径安全:** 始终使用绝对路径或 `os.path.join()`、`Path()` 拼接路径，不依赖 CWD。
*   **编码安全:** `.bat` 和 `.ps1` 脚本内容尽量使用 ASCII 字符，避免 Windows 终端编码问题。
*   **空值检查:** 访问对象属性前始终验证对象是否存在。
*   **目录安全:** 使用 `os.path.dirname(os.path.abspath(__file__))` 获取脚本所在目录作为基准路径。

## 10. File & Material Management（文件与材料管理）
**核心铁律: 自动发现、严格校验。**
*   **材料自动发现:** 使用 `glob.glob()` 按命名规则自动定位 TXT 信息文件、源代码 PDF、文档 PDF 等申请材料。
*   **缺失检测:** 找不到必需文件时立即抛出 `FileNotFoundError` 并给出清晰的文件名要求提示。
*   **文本解析:** 使用正则表达式从 TXT 信息文件中提取结构化数据（软件全称、版本号、编程语言等），支持 UTF-8 和 GBK 编码自动切换。
*   **调试文件:** 调试中间产物（`debug_*.png`、`debug_*.txt`）保存在项目根目录，方便排查但不参与打包。

## 11. Verification Before Completion（完成前验证）
**核心铁律: 无证据不宣告完成。**
*   **运行验证:** 声明"完成"之前，必须实际运行程序/脚本并观察输出。
*   **禁止"应该可以":** 不允许使用"应该没问题"、"看起来正确"等措辞，必须有运行证据。
*   **截图证据:** UI 变更需截图确认最终效果；自动化流程需记录关键步骤的日志输出。
*   **回归检查:** 修改后确认两种功能模式（监控/上传）都能正常启动和运行。

## 12. Project Architecture（项目架构约定）
**核心铁律: 统一入口，模块解耦。**
*   **统一入口:** `main.py` 为唯一入口，初始化 ttkbootstrap Window 和 GUI。
*   **GUI 层:** `gui_main.py` 负责界面布局和用户交互，不包含业务逻辑。
*   **业务层:** `AppCore` 类负责编排业务流程，调用各功能模块。
*   **功能模块:**
    - `page_judger.py` — 页面状态识别与 OCR 数据提取
    - `navigator_r11.py` — R11 登记表单自动化导航与填写
    - `exporter.py` — 数据对比、Excel 导出与截图保存
    - `browser_utils.py` — 浏览器启动与管理
    - `config_manager.py` — 配置文件读写
*   **配置共享:** 两种功能模式共享同一个 `config.json`，包含 `account/username`、`password`、`delay_rate` 等字段。
