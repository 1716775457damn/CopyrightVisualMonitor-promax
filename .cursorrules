# CopyrightVisualMonitor 混合架构开发规约 (Rules)

基于 `rust-pro`、`systems-programming-rust-project`、`python-pro` 及 `playwright-skill` 的核心准则，本项目由于采用了 **Rust + Python(Airtest/Playwright)** 边车混合架构，特制立以下开发规约：

## 1. Rust 系统工程准则 (Rust Pro & Systems Scaffolding)
- **严格架构分层**：
  - 维持 `src/` 下的标准模块化（如 `main.rs`, `gui.rs`, `bridge.rs`, `error.rs`）。
  - 不得在主线程中执行阻塞进程的代码。凡是涉及到与 Python 边车进行 IPC 通讯的指令，必须使用 `tokio` 异步运行时包裹。
- **内存安全与错误处理**：
  - 使用 `anyhow` 包裹向上传递的不可恢复异常，使用 `thiserror` 定义强类型的库级错误（如 `BridgeError::PythonProcessDead`）。
  - 禁止滥用 `unsafe` 块；必须保证系统稳定性不因 UI 假死而崩溃。
- **现代化构建**：
  - 禁止使用老旧的批处理文件。一切构建由 `cargo build --release` 以及 `Makefile` 接管。

## 2. Python 边车与视觉制导 (Python Pro & Browser Automation)
- **无状态流式通信 (IPC)**：
  - Python 子工程 (`src-airtest/runner.py`) 应当是绝对无状态的。一切全局状态都必须存于 Rust 内存。
  - 数据传输格式极其严苛：必须使用标准化的逐行 JSON (JSONL) 进行 `stdin` 和 `stdout` 的通信，务必要包裹 `try..except` 清洗任何不干净的控制台输出，防止 `JSONDecodeError` 破坏 Rust 管道。
- **混合驱动策略**：
  - 面向稳定 DOM 结构：继续强制使用 Playwright (基于 CDP) 下发超音速操作指令。遵循 `playwright-skill` 最佳实践。
  - 面向复杂验证码及反爬重灾区（登录及滑块）：采用 Airtest 特征图匹配 `touch(Template())` 进行物理级降维接管。

## 3. UI 与交互 (Tauri / Egui) 
- Rust 端的 GUI 输出应当对等原先 Tkinter 的日志输出。一切界面的重绘必须非阻塞。

请在后续所有的代码重构和维护中严格准守此份 Rule，以保证双语言编译栈的纯粹与解耦。 
